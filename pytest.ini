[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    live: marks tests that interact with live Gemini API (deselect with '-m "not live"')
env =
    DATABASE_URL=postgresql://gemini_bot:gemini_bot_password@localhost:5433/gemini_bot_test 